services:
  - type: web
    name: gravilog
    env: python
    plan: free
    buildCommand: pip install -r pyproject.toml
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000